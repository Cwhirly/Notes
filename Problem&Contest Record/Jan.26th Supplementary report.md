# Timeline
5 min 打完 A，B 看了好久先放，C、D 都是模板，共花费 50 min 左右。  
然后发现 F 也是板。  
最后打表把 E 打出来了。  
看 G、H，觉得 H 不可做，就全力攻击 G，只剩 30 min，疯狂写，最后 lower_bound 还忘了细节，调了许久未果，最后不管了交一下 10 pts，结果比赛已经结束。
# ~={green}A=~
贪心，每次找最小的两个，合并之后再放回去。  
要求快速找最小，于是可以使用优先队列维护。  
（当时忘了 `vector<int>` 和 `greater<int>` 哪个在前那个在后了，还试了两遍……）  
# ~={cyan}B=~
发现把第一个位置定了，后面的就全部能推出来。  
枚举第一个有雷或没雷即可。
# ~={green}C=~
由反证法或由 Kruskal 原理，我们可以得出，最小生成树的最长边最小。
于是直接 Kruskal：  
以边权为关键字，排序后并查集连边，避免环的出现即可。
把答案的转移从“+”改成“$\max$”即可。
# ~={green}D=~
单调栈板子。
# ~={green}E=~
考场时打表打出来的，证明其实推个式子即可。
# ~={green}F=~
树 DP 板子。
设 $dp(i,0/1)$ 为 $i$ 去/不去，$i$ 的子树的答案。  
$dp(i,0)=\sum \max(dp(son,0),dp(son,1))$
$dp(i,1)=\sum dp(son,0)$
# ~={red}G=~
# ~={red}H=~
