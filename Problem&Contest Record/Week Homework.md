## Week 52
### A. CF 1730 B Div. 2 
省流：
给第一象限上的若干点，求一个 $A(x,0)$，使得最小化所有点到 $A$ 的曼哈顿距离的最大值，$x$ 是正实数。
思路：  
肯定是二分答案。曼哈顿距离中圆是一个斜的正方形，正半轴上就是一个等直。  
经典 trick 旋转坐标系？没那么复杂。  
等直的直角顶点在 $l:y=mid$ 上运动，所以过它做 $y=x+c_1,y=-x+c_2$ 看能不能包裹所有点。那是不是不用二分？找两边取个交点就做完了。
先写吧。样例过了？  
Feel Scared 了，WA on #3 。哦没写 `setprecision` 招笑了。
过了，不是哥们。

### B. CF 1714 G Div. 3
省流：
给一棵树，两种边权，问每个节点到根的路径的最长前缀使得该前缀的第二种边权和大于整条路径的第一种边权和。
思路：
一眼秒了，这不直接树上二分，用预处理 LCA 的那玩意做就行，$O(n\log n)-O(n\log n)$，不管了估计没有不带 $\log$ 做法。
欸不对这是双 $\log$。哦不需要倍增。
那很容易了。

### C. CF 1630 B Div. 1
省流：
把一个序列分成 $k$ 段，使得每一段中处于 $[x,y]$ 中的数个数严格多于不处于 $[x,y]$ 中的数的个数。最小化 $y-x$，给出分段方案。
思路：
考虑枚举 $x,y$，这玩意可以双指针就是 $O(n)$。  
那这就代表着 check 必须是 $o(n)$。  
其实就转化为 01 序列分段使得每段 1 的个数大于 0 的个数。  
那其实跟序列顺序无关，那维护 0/1 的个数即可。
开写。  
被样例创飞了 1145141919810 年，呃呃呃。
怎么还 TLE on #2 了！啊原来是每组数据都开了 $10^5$ 的 `vector`。服了。

### D. CF 1744 E_2 Div. 3
省流：
给 $4$ 个数 $a,b,c,d$，找到一个 $x,y$，使得 $a\le x\le b\wedge c\le y\le d\wedge ab|xy$，或报告无解。  
思路：
肯定是要做质因数分解或者是枚举因数，反正 $10^9$ 只要是 $o(n)$ 应该都能过。  
不对那枚举因数就不行，乘到一起得有 $10^{18}$ 了。  
不过先考虑枚举因数怎么做……哦是不是只需要枚举 $a$ 的因数？似乎是的。似乎不是。 
哦那也枚举 $b$ 的因数，然后乘一块，$O(d^2)$ 问题不大。
没救了被样例肘飞了，哦哦我沼泽了，OK 啊一发过了。

### E. CF 1715 D Div. 2
省流：
给你若干个形如 $a_i|a_j=x$ 的限制，还原出字典序最小的 $a$。  
思路：
肯定先拆位，然后能确定一些 $0$ 的位出来。  
剩下的就变成一个看起来 2-SAT 的东西。  
啊啊啊 2-SAT 咋做忘了没救了，看一眼模板。  
这个字典序的话，我们肯定是希望越靠前且越高位的尽可能是 $0$。  
直接贪啊！？好像还真是，不是哥们！  
吃到自助餐了！啊啊啊！
虫勾然后过了。  
的世界。

### F. CF 1630 C Div. 1
省流：
给你一个全是白色的序列，每次可以选择一段区间使得 $a_l=a_r$，然后把区间内非端点的都染黑，问最后最多有几个黑的。  
思路：
首先线段有包含肯定没用。  
相交的话发现其实整体左端点到整体右端点中间大部分都能染黑，只有某些染不黑。  
发现这些都是端点。手玩一下……？哦，对于一堆覆盖连续范围的线段，整个连续范围内染不黑的点的数量就是线段数 $+1$。那就是让覆盖的线段尽可能少。  
用尽可能少的线段覆盖特定的某些点？超速检测！  
欸嘿粘贴一下之前的 AC 代码。  
我去样例不过。  
哦它本质上并不是超速检测，好好好。写完之后怎么样例还是过不了。  
原来贪心策略有问题，改。调了 33550336 辈子终于调出来力！WA  on #3 诗人？  
我服了，哦细节还有问题，再改。不是怎么还吃，真的 Feel Scared 了。 
继续改，已经改成依托了。似乎明白了什么。我懂了，但是好难写啊。
啊啊啊好乱，让大师看看。哇大师的写法确实高超简洁，不过大师都能干咱们作为人也能干，这种事情不用气馁。
按照大师的写法通过力！

### G. Luogu 9016 Blue
省流：
初始字符串 $S=$`a`，每次操作将其中某字符全替换成某个字符串，输出最后的 $S_{l…r}$。   
​思路：
字符串最后的大小是指数级的。并且每次字符串都会变长。那是不是只需要考虑 $r$ 以内的即可？但是 $r$ 会很大。  
先手模一下样例或许能发现什么。反着考虑？不像。从长度出发？也不好搞。  
或许从树形出发？就是问第 $l\sim r$ 个叶子是什么。把树形画出来看看。发现每层的节点在下一层会变成该字母接下来第一个变为的字符串，所以还需要记录该节点是处于第几次操作后的状态。那么，对于同一个字母来说，它有效的时间就只有对它进行替换的那若干次操作后。所以这表明本质不同的节点数不会超过 $q$，好像是废话。但是本质相同的节点有哪些儿子也是确定的。那么把这些点合并起来？不行，树的儿子跟顺序有关系。  
所以这并不是一个标准意义上的树。能不能把这个变成标准意义的树？啥东西和顺序有关系？呃。  
啊啊不会啊。【ZJR】题解。哦是二叉树！把这玩意变成 Leafty 的结构。  
我去这么精妙。

### H. ARC 120 D
省流：
给你一个序列，求一个与之长度相等的括号序列，使权值最大。权值为原序列匹配的括号的位置的元素的差的绝对值之和。  
思路：
最好的肯定是 $k$ 大值匹配 $k$ 小值。所以看怎么调整括号序列。  
手玩一下样例发现了华点，其实怎么搞都不变，那么怎么会变？大神 Qaaxaap 指出，只要前一半小的匹配上后一半大的就可以了！啊怎么这么强！  \o/\o/\o/\o/\o/\o/\o/\o/\o/\o/\o/\o/\o/  
哦其实也比较显然的吧……？  
你说得对。  
那么怎么构造这种匹配呢？直接开个栈扫一遍就行！  

### I. CF 1766 E Educational
省流：
有点长，不好省流，直接放链接吧。[CF 链接](https://codeforces.com/problemset/problem/1766/E)。
思路：
手模一下样例，发现了一些性质，DP，做完了。（bushi

## Week 53
估计写不完了。

### A. CF 1707 A Div. 1
写的时候没记思路，太简单了，忘了。  

###  B. CF 1672 D Div. 2
省流：
每次选择一个序列 $a$ 的某两个位置 $l,r\ s.t.a_l=a_r$，然后删掉 $a_l$，再把 $a_r$ 复制一份，这样的操作可执行任意次。问最后能不能变成另一个序列 $b$。  
思路：
显然是把 $a\to b$ 转化成 $b\to a$。  
然后 $b$ 能变的只有相邻的相同数字，每一个相同数字连续段的右端点保留，剩下的可以随便往前扔。  
既然只能往前扔，那我们就从后往前考虑？不行吧。  
哦，那是不是说，只要 $a$ 里面所有数出现位置的右端点的顺序和 $b$ 里面的一样就可以了？  
手膜感觉很对，先写一发。我什么时候才能用对 `i64` 和 `usize` 啊啊啊。
WA on #2 了，看来不那么对。就是没那么对。呃，就当练习 Rust 了。  
哦哦哦我知道锅在哪了，我服了，就这玩意还能跑到 #2 的第 32 个点也是奇迹 。  
于是改成了双指针，于是就盾了。

### C. CF 1935 D 
省流：
给一个集合 $S$ 和一个整数 $c$，问有多少对 $x\le y$ 使得 $y+x,y-x \notin S$。  
思路：
不会是直接预处理每个数前面和后面的奇数与偶数个数然后就做完了吧……  
欸不行，间隔太大了没办法枚举。那就容斥一下，考虑有多少不满足条件。首先都在集合中是容易统计的，只有 $y+x$ 在集合中的以及只有 $y-x$ 在集合中的，呃，好像也很简单……吗？哦，是的，二次容斥，考虑 $y+x$ 在集合中的，$y-x$ 在集合中的，加一块，然后再减去都在集合中的就行。  
那不糖丸了吗，写写写。  
样例没过，不明白，先交一发再说，欸 tmd 咋 AC 了。

### E. CF 1718 A2 Div. 1
省流：
给你一个序列，每次把序列中的一段异或上同一个值，花费 $\left\lceil\frac{len}{2}\right\rceil$，问最少花费多少才能把序列变成 $0$。  
思路：
首先肯定是选偶数长度的段比较优。  
然后这玩意所有偶数都是等价的，奇数也是等价的，所以可以拆，就拆成若干个 $2$ 和若干个 $1$。长度为 $1$ 的话好办，直接异或自己变成 $0$ 即可。$2$ 的话不好搞，但是感觉这东西很 DP。那么 DP 怎么设？  


### F. ARC 125 D
省流：
问一个序列的只出现过一次的子序列个数。   
思路：
如果把子序列改成子串那就是裸的 SAM，可惜不是。欸是不是有个东西叫做序列自动机来着，不会是这东西的板子吧！OI-wiki 翻一下看看。呃好吧它好像不能处理这个。  
看来还是想点正经的 DP 做法吧。
差不多了吧，我不会啊。  
欸等等是不是我想到这一步已经想完了来着？  
写写写。哎，不是，干什么，样例不过。  
对着虚空调试了一万年，发现直接用树状数组隐式转移 DP 不可过，必须把 DP 数组显式写出来才行，原因不明。
我真服了。